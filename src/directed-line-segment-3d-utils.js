import DirectedLineSegment2DUtils from "./directed-line-segment-2d-utils";

export default {
  connect(originalLineSegments) {
    return DirectedLineSegment2DUtils.connect(originalLineSegments);
  }
};
